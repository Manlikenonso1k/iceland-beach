name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 92.112.197.130
          username: u519226541
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 65002
          script_stop: true
          script: |
            cd domains/icelandbeach.com/public_html
            git pull origin main