Task: Fix Filament Calendar UI and Migrate Legacy Room Data.

Current Issue: The Booking page is empty ("no rooms") and lacks the Google Calendar-style UI.

Instructions:

Data Bridge: Read the legacy rooms.php and core/ files. Create a Laravel Seeder that extracts the room types and pricing from the legacy PHP arrays/DB and inserts them into the new Laravel rooms table.

Calendar Fix: >    - Install saade/filament-fullcalendar.

Create a BookingCalendarWidget.

Ensure the BookingResource is configured to use this widget as the default view instead of a standard table list.

Role Management:

Set up Spatie Permission or Filament Shield.

Create roles: Super Admin, Manager, Front Desk, and Waiter.

Restrict the "Confirm/Reject" booking actions to only Super Admin and Manager.

Room Selection: In the Booking form, create a Select component that fetches options dynamically from the Room model.

Local to Production Safety: Ensure all file paths and DB connections use .env variables so the code works on localhost now and the production server later without manual edits.