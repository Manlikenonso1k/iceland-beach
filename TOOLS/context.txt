Role: Senior Laravel & Filament Developer

Task: Refactor the "icelandbeach" Filament admin dashboard (/admin/bookings) to improve booking management and legacy data integration.

Current Setup: > * Filament Admin Panel with two Google-style calendar widgets.

A legacy rooms.php file/system that handles date and time bookings.

Requirements:

1. Dashboard Layout Change:

Replace one of the two calendar widgets with a "Pending Bookings" Notification Widget.

This widget should list recent booking requests with two primary actions: Confirm and Reject.

2. Interactive Actions & Email:

When the "Confirm" button is clicked: Update the booking status to 'confirmed' and trigger an automated "Booking Confirmed" email to the user.

When the "Reject" button is clicked: Open a modal for an optional reason, update the status to 'rejected', and send a "Booking Declined" email.

3. Legacy Integration (rooms.php):

The remaining Calendar widget must now pull data from the tables associated with the legacy rooms.php.

Ensure the calendar displays these bookings using color-coding based on room type (e.g., Deluxe = Blue, Suite = Gold).

Use abbreviations for room names (e.g., "Deluxe Room 101" becomes "DR-101") to keep the calendar view clean.

4. Visual Logic:

Only "Confirmed" bookings should show as solid color blocks on the calendar.

Provide the code for the Filament Resource, the Custom Widget, and the Mailables required.